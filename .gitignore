.godot/
*.uid
# Anchored to repo root so it only ignores Godot's Android build template,
# not asset dirs like assets/android/.
/android/
export_presets.cfg

# Release signing — NEVER commit these
*.keystore
*.jks

# Export output
build/
